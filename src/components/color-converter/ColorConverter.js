import React from "react";
import styles from "../../css/color-converter/ColorConverter.module.scss";
import { PropTypes } from "prop-types";

// FEATURES:
// - Supports all conversions:
// 		- Hex to: RGBA, HSLA
// 		- RGBA to: HEX, HSLA
// 		- HSLA to: HEX, RGBA
// - Supports copying all color code(s) formats
// - Supports color preview card
// - Supports CSS code window w/ copyable code using color

const ColorConverter = () => {
	return (
		<div className={styles.ColorConverter}>
			{/*  */}
			{/*  */}
			{/*  */}
		</div>
	);
};

export default ColorConverter;

ColorConverter.defaultProps = {};

ColorConverter.propTypes = {};
